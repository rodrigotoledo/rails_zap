# config/sidekiq.yml

development:
  :concurrency: 5
  :url: redis://redis:6379/1 # Use o nome do serviço do contêiner Redis
  :namespace: myapp_sidekiq

production:
  :concurrency: 10
  :url: redis://redis:6379/1 # Use o nome do serviço do contêiner Redis
  :namespace: myapp_sidekiq

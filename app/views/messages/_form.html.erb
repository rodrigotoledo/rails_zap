<%=form_for :message, url: conversation_messages_path(conversation.id), method: :post, class: 'w-full', data: { controller: "reset-form", action: "turbo:submit-end->reset-form#reset"} do |form| %>
  <%=form.text_field :message, placeholder: 'Digite uma mensagem', class: 'w-full bg-transparent border-transparent border-0 focus:border-[#1f2c32] text-[#aebac1] h-7 focus:outline outline-none', data: {controller: "message", reflex: "keyup->message#typing", conversation_id: conversation.id}, autocomplete: 'off' %>
<% end %>